server:
  port: 8080
  baseURL:  "/files/"
  logging:
    - levels: "info|error|debug"
  sources:
    - path: "../frontend-next/tests/playwright-files"
      name: "playwright"
      config:
        defaultEnabled: true
